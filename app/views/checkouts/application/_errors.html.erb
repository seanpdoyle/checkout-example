<%= tag.p(
  id: "#{form.object_name}_#{attribute}_errors",
  class: "
    text-red-500
  ",
  "data-form-target": "error",
  **local_assigns.except(:form, :attribute)
) do %>
  <%= form.object.errors[attribute].to_sentence %>
<% end %>
