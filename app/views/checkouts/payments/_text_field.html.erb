<%= tag.div(
  id: "#{form.object_name}_#{attribute}",
  "aria-describedby": "#{form.object_name}_#{attribute}_errors",
  class: "
    form-input
  ",
  **local_assigns.except(:form, :attribute),
) %>
